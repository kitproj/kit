apiVersion: kit/v1
kind: Tasks
metadata:
  annotations:
    help: https://github.com/kitproj/kit
  name: kit
spec:
  tasks:
  - name: foo
    ports: "8080"
  - name: bar
    sh: echo bar
    dependencies: baz qux