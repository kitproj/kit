apiVersion: v1
kind: Pod
metadata:
  name: dev
spec:
  containers:
    - name: foo
      command: [ go, run, ./demo/foo ]
      readinessProbe:
        initialDelaySeconds: 10
        httpGet:
          port: 8080
    - name: bar
      command: [ go, run, ./demo/bar ]
      env:
        - name: PORT
          value: "9090"
      readinessProbe:
        initialDelaySeconds: 10
        httpGet:
          port: 9090

